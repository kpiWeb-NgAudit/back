# ===================================================================
# SQL SERVER DATASOURCE CONFIGURATION
# ===================================================================
spring.datasource.url=jdbc:sqlserver://PC_KPIWEB_STG:1433;databaseName=KPIW_1.5;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=kpiWeb
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Optional: JPA/Hibernate properties (if you are using Spring Data JPA)
# spring.jpa.hibernate.ddl-auto=update # Be careful with this in production (validate, none, create, create-drop)
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServer2012Dialect
# For newer SQL Server versions, you might use SQLServer2016Dialect or a more recent one.